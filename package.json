{
  "name": "calc",
  "version": "1.0.0",
  "private": true,
  "description": "Calculator app wrapped with Capacitor JS",
  "main": "index.html",
  "scripts": {
    "build": "node -e \"const fs=require('fs');const p=require('path');fs.mkdirSync('www',{recursive:true});['index.html','style.css','app.js'].forEach(f=>fs.copyFileSync(f,p.join('www',f)));console.log('Copied web assets to www/')\"",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
